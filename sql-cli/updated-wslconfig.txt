[wsl2]
# Increase from 12GB to 32GB for WSL2 (you have 64GB total)
memory=32GB

# Keep 8 processors or increase if you have more cores
processors=8

# Add swap for better memory management
swap=16GB

# Optional: specify swap file location if C: drive is low on space
# swapFile=D:\\WSL\\swap.vhdx

# Additional performance settings
localhostForwarding=true
nestedVirtualization=true  # Important for Docker
pageReporting=false  # Can improve performance

# Save this as C:\Users\sjame\.wslconfig (replacing the current one)
# Then in PowerShell (Admin): wsl --shutdown
# Wait 5 seconds, then start WSL2 again